import { FilterModelsListPipe } from './filter-models-list.pipe';

describe('FilterModelsListPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterModelsListPipe();
    expect(pipe).toBeTruthy();
  });
});
